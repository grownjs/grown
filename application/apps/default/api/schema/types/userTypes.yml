id: userTypes

definitions:
  accessRole:
    type: string
    enum:
      - ROOT
      - ADMIN
      - USER
      - GUEST
  accessType:
    type: string
    enum:
      - VALIDATE_EMAIL
      - RECOVER_PASSWORD
